using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class skryptZgniatacza : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    /*
    public void OnTriggerEnter(Collider other)
    {
        if(other.tag=="blacha")
        {
          //  other.gameobject.
                other.gameObject.GetComponentInParent<Dane>().manipulowanyObiekt.gameObject.GetComponent<MeshFilter>().sharedMesh = other.gameObject.GetComponent<obslugaBlachy>().meshe[3].sharedMesh;
        }
    }*/
}
